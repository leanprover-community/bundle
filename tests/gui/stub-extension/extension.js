// Minimal stub extension for the test harness.
// The actual tests run via --extensionTestsPath; this just satisfies
// the --extensionDevelopmentPath requirement.
function activate() {}
function deactivate() {}
module.exports = { activate, deactivate };
